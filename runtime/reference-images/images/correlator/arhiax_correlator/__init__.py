"""arhiax_correlator — package marker.

Public symbols are re-exported lazily by the modules below; this file
intentionally stays empty so that `import arhiax_correlator` does not
trigger any side effects (no logger config, no thread spawn, no env
read). All initialization happens in main.py.
"""
